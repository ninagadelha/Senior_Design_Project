import React from 'react';

// const MockNavBar = jest.fn(() => <div data-testid="mock-navbar">Mock NavBar</div>);

const MockNavBar = () => {
    <div data-testid="mock-navbar">Mock NavBar</div>
  };

export default MockNavBar;
