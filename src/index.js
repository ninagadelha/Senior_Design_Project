import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Ensure you have an App.js component in the same directory

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
